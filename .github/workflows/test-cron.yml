name: Experiment
on: 
  workflow_dispatch:
    branches: [ "main", "prod" ]
  schedule:
  - cron: '*/5 * * * *'

jobs:
  job1:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: echo hello world